# Copy this file to secrets.yaml and fill in your actual values
# secrets.yaml is in .gitignore to prevent accidentally committing credentials

# WiFi Credentials
wifi_ssid: "Your WiFi Network Name"
wifi_password: "Your WiFi Network Password"

# Home Assistant (Optional - only if not using discovery)
# ha_url: "http://192.168.1.100:8123"
# ha_token: "your-long-lived-access-token"

# OTA Password (Optional - for Over-The-Air updates)
# ota_password: "secure-password-for-ota"
